name: wo_form_service
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  cached_network_image: ^3.4.1
  collection: ^1.18.0
  dotted_border: ^2.1.0
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.5
  http: ^1.2.2
  image_cropper: ^8.0.2
  image_picker: ^1.1.2
  path_provider: ^2.1.5
  shimmer: ^3.0.0
  wo_form: ^1.2.1

dependency_overrides:
  wo_form:
    path: ../wo_form

dev_dependencies:
  very_good_analysis: ^6.0.0

flutter:
